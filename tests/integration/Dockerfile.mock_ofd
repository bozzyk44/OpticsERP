# Dockerfile for Mock OFD Server
# Purpose: Lightweight Flask server simulating OFD API
#
# Build: docker build -f Dockerfile.mock_ofd -t mock_ofd:latest .
# Run:   docker run -p 9000:9000 mock_ofd:latest

FROM python:3.11-slim

# Set working directory
WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements
COPY requirements-mock.txt /app/

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements-mock.txt

# Copy mock OFD server code
COPY mock_ofd_server.py /app/
COPY mock_ofd_standalone.py /app/

# Expose port
EXPOSE 9000

# Set environment variables
ENV FLASK_ENV=development \
    OFD_PORT=9000 \
    OFD_HOST=0.0.0.0 \
    PYTHONUNBUFFERED=1

# Health check
HEALTHCHECK --interval=10s --timeout=5s --retries=5 --start-period=5s \
    CMD curl -f http://localhost:9000/ofd/v1/health || exit 1

# Run standalone server
CMD ["python", "mock_ofd_standalone.py"]
