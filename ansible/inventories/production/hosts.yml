---
# OpticsERP Production Inventory
# MVP Configuration: All-in-one server (all components on one host)

all:
  children:
    # All server groups point to the same physical server for MVP
    odoo_servers:
      hosts:
        mvp-server:
          server_role: primary

    kkt_adapters:
      hosts:
        mvp-server:
          pos_location: store_01

    db_servers:
      hosts:
        mvp-server:

    monitoring_servers:
      hosts:
        mvp-server:

  # Global variables for all hosts
  vars:
    ansible_python_interpreter: /usr/bin/python3
    deployment_env: production

    # SSH connection settings
    ansible_host: 194.87.235.33
    ansible_user: bozzyk44
    ansible_port: 22
    ansible_ssh_private_key_file: ~/.ssh/opticserp

    # Note: ansible_become is set to 'yes' in playbooks where sudo is needed
    # For passwordless sudo, configure on server: user ALL=(ALL) NOPASSWD:ALL
